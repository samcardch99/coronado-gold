---
const pathname = Astro.url.pathname;
const isHome = pathname === "/" || pathname === "/index.html";
---

<div
    id="nav-container"
    class="fixed bottom-4 md:bottom-10 left-1/2 -translate-x-1/2 transition-all duration-700 ease-in-out flex justify-between items-center h-12 md:h-24 w-[90%] md-custom:w-3/5 lg:w-3/5 xl:w-2/5 bg-[#a4a4a400] backdrop-blur-lg text-darkRed text-sm md:text-3xl rounded-full z-500 mx-auto overflow-visible"
    style={{
        border: "1px solid rgba(255, 255, 255, 0.4)",
        boxShadow: [
            "0 15px 25px -5px rgba(0, 0, 0, 0.15)",
            "0 8px 10px -6px rgba(0, 0, 0, 0.1)",
            "inset 2px 2px 4px rgba(255, 255, 255, 0.7)",
            "inset -2px -2px 5px rgba(0, 0, 0, 0.05)",
        ].join(", "),
    }}
>
    <!-- noise overlay cristal -->
    <span
        class='pointer-events-none absolute inset-0 z-1 rounded-full opacity-[0.04] mix-blend-overlay [background-image:url(&apos;data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%"><filter id="noise"><feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"/></filter><rect width="100%" height="100%" filter="url(%23noise)"/></svg>&apos;)]'
    ></span>

    <div
        id="active-indicator"
        class={`absolute top-0 left-0 h-full w-1/2 md:w-1/3 transition-all duration-500 ease-[cubic-bezier(0.23,1,0.32,1)] pointer-events-none z-0 scale-102 ${isHome ? "opacity-100" : "opacity-0"}`}
    >
        <div
            class="w-full h-full bg-[#d9d9d90f] rounded-full relative overflow-hidden"
            style={{
                border: "1px solid rgba(255, 255, 255, 0.4)",
                boxShadow: [
                    "0 15px 25px -5px rgba(0, 0, 0, 0.15)",
                    "0 8px 10px -6px rgba(0, 0, 0, 0.1)",
                    "inset 2px 2px 4px rgba(255, 255, 255, 0.7)",
                    "inset -2px -2px 5px rgba(0, 0, 0, 0.05)",
                ].join(", "),
                transition:
                    "box-shadow 700ms ease-in-out, border 700ms ease-in-out",
            }}
        >
            <!-- noise overlay cristal -->
            <span
                class='pointer-events-none absolute inset-0 z-1 rounded-full opacity-[0.04] mix-blend-overlay [background-image:url(&apos;data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%"><filter id="noise"><feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"/></filter><rect width="100%" height="100%" filter="url(%23noise)"/></svg>&apos;)]'
            ></span>
        </div>
    </div>

    <button
        data-index="0"
        class="hidden md:block nav-btn relative z-10 tracking-wide flex-1 h-full transition-opacity duration-500"
    >
        Home
    </button>
    <button
        data-index="1"
        class="nav-btn relative z-10 tracking-wide flex-1 h-full transition-opacity duration-500"
    >
        Categories
    </button>
    <button
        data-index="2"
        class="nav-btn relative z-10 tracking-wide flex-1 h-full transition-opacity duration-500"
    >
        Most Popular
    </button>
</div>

<style>
    button:hover {
        cursor: pointer;
    }
</style>
<script>
    const navContainer = document.getElementById("nav-container");
    const indicator = document.getElementById("active-indicator");
    const buttons = document.querySelectorAll(".nav-btn");

    const sectionMapping = {
        "0": "home",
        "1": "categories",
        "2": "most-popular", // Most Popular component ID
    };

    const currentPath = window.location.pathname;
    const isHomePage = currentPath === "/" || currentPath === "/index.html";

    // Actualiza dinámicamente el desplazamento basándose solo en los botones visibles
    function updateIndicator(targetIndexStr) {
        if (!indicator) return;

        let visualIndex = null;
        let visibleCount = 0;

        buttons.forEach((b) => {
            if (window.getComputedStyle(b).display !== "none") {
                if (b.getAttribute("data-index") === targetIndexStr) {
                    visualIndex = visibleCount;
                }
                visibleCount++;
            }
        });

        // Si el botón está desactivado en la vista actual (ej. Home en mobile) o no existe, oculta y aborta
        if (visualIndex === null) {
            indicator.classList.remove("opacity-100");
            indicator.classList.add("opacity-0");
            return;
        }

        indicator.classList.remove("opacity-0");
        indicator.classList.add("opacity-100");
        indicator.style.transform = `translateX(${visualIndex * 98}%)`;
    }

    const indicatorInner = indicator?.querySelector(
        "div",
    ) as HTMLElement | null;

    const shadowDefault = [
        "0 15px 25px -5px rgba(0, 0, 0, 0.15)",
        "0 8px 10px -6px rgba(0, 0, 0, 0.1)",
        "inset 2px 2px 4px rgba(255, 255, 255, 0.7)",
        "inset -2px -2px 5px rgba(0, 0, 0, 0.05)",
    ].join(", ");
    const shadowSubtle = [
        "0 15px 25px -5px rgba(0, 0, 0, 0.15)",
        "0 8px 10px -6px rgba(0, 0, 0, 0.1)",
        "inset 2px 2px 4px rgba(255, 255, 255, 0.2)",
        "inset -2px -2px 5px rgba(0, 0, 0, 0.05)",
    ].join(", ");

    function updateBarTheme(targetIndexStr: string) {
        if (!navContainer) return;
        const isCategories = targetIndexStr === "1";

        // Colores de fondo y texto
        if (isCategories) {
            navContainer.classList.remove("bg-[#a4a4a400]", "text-darkRed");
            navContainer.classList.add("bg-[#2D1B15]/60", "text-[#EBE6E0]");
        } else {
            navContainer.classList.remove("bg-[#2D1B15]/60", "text-[#EBE6E0]");
            navContainer.classList.add("bg-[#a4a4a400]", "text-darkRed");
        }

        // Relieve cristal: más sutil en categorías (fondo oscuro), normal en el resto
        const shadow = isCategories ? shadowSubtle : shadowDefault;
        const border = isCategories
            ? "1px solid rgba(255, 255, 255, 0.2)"
            : "1px solid rgba(255, 255, 255, 0.4)";

        navContainer.style.boxShadow = shadow;
        navContainer.style.border = border;
        if (indicatorInner) {
            indicatorInner.style.boxShadow = shadow;
            indicatorInner.style.border = border;
        }
    }

    let isScrollingViaClick = false;
    let scrollTimeout = null;

    // 1. Scroll directly to sections on click
    buttons.forEach((btn) => {
        btn.addEventListener("click", () => {
            const index = btn.getAttribute("data-index");
            if (!index) return;

            const targetId =
                sectionMapping[index as keyof typeof sectionMapping];

            if (!isHomePage) {
                // Redirect user to the home page at the specific anchor
                window.location.href = `/#${targetId}`;
                return;
            }

            // Actualizar estilo visual manual temporalmente y pausar observer
            isScrollingViaClick = true;
            if (scrollTimeout) clearTimeout(scrollTimeout);
            scrollTimeout = setTimeout(() => {
                isScrollingViaClick = false;
            }, 1200);

            updateIndicator(index);
            updateBarTheme(index);

            const targetSection = document.getElementById(targetId);

            if (targetSection) {
                targetSection.scrollIntoView({ behavior: "smooth" });
            }
        });
    });

    if (isHomePage) {
        // 2. Intersection Observer to detect scroll position
        const observerOptions = {
            root: null,
            rootMargin: "0px",
            threshold: 0.4, // Update when at least 40% of standard sections are in view
        };

        let visibleSections = new Set();

        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    visibleSections.add(entry.target.id);
                } else {
                    visibleSections.delete(entry.target.id);
                }
            });

            if (visibleSections.size === 0) {
                if (!isScrollingViaClick) {
                    indicator?.classList.remove("opacity-100");
                    indicator?.classList.add("opacity-0");
                    updateBarTheme("0"); // ninguna sección tracked visible → tema claro
                }
            } else {
                if (isScrollingViaClick) return;

                // Toma la más reciente que haya entrado al viewport
                const topVisibleId = Array.from(visibleSections).pop();
                let foundIndex = null;
                for (const [idx, id] of Object.entries(sectionMapping)) {
                    if (id === topVisibleId) {
                        foundIndex = idx;
                        break;
                    }
                }

                if (foundIndex !== null) {
                    updateIndicator(foundIndex);
                    updateBarTheme(foundIndex);
                }
            }
        }, observerOptions);

        // Observer attachments
        Object.values(sectionMapping).forEach((id) => {
            const el = document.getElementById(id);
            if (el) observer.observe(el);
        });
    }

    // 3. Footer Observer for global graceful hiding
    const footerElement = document.getElementById("footer");
    if (footerElement && navContainer) {
        const footerObserverOptions = {
            root: null,
            rootMargin: "0px",
            threshold: 0.1, // 10% of the footer
        };

        const footerObserver = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    navContainer.classList.add(
                        "opacity-0",
                        "translate-y-10",
                        "pointer-events-none",
                        "scale-95",
                    );
                } else {
                    navContainer.classList.remove(
                        "opacity-0",
                        "translate-y-10",
                        "pointer-events-none",
                        "scale-95",
                    );
                }
            });
        }, footerObserverOptions);

        footerObserver.observe(footerElement);
    }

    // 4. Ocultar/mostrar cuando se abre/cierra el sidebar de categorías
    document.addEventListener("sideMenuOpen", () => {
        navContainer?.classList.add(
            "opacity-0",
            "translate-y-10",
            "pointer-events-none",
            "scale-95",
        );
    });

    document.addEventListener("sideMenuClose", () => {
        navContainer?.classList.remove(
            "opacity-0",
            "translate-y-10",
            "pointer-events-none",
            "scale-95",
        );
    });
</script>
